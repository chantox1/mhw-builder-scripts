Reads .gmd files in the input directory and outputs them as .json files in a site-friendly directory structure.
Expects filenames matching *_{lang_code}.gmd, where lang_code is the three-letter language identifier (ara, eng, etc.)
